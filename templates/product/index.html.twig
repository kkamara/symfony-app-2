{% extends 'base.html.twig' %}

{% block title %}All Products{% endblock %}

{% block body %}

<h1>Products</h1>

<a href="{{ url('product_new') }}">New Product</a>

<table>
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th>Description</th>
            <th>Size</th>
        </tr>
    </thead>
    <tbody>
        {% for product in products %}
        <tr>
            <td>
                <a href="{{ url('product_show', {'id': product.id}) }}">
                    {{ product.name }}
                </a>
            </td>
            <td>{{ product.description }}</td>
            <td>{{ product.size }}</td>
        </tr>
        {% endfor %}
    </tbody>
</table>

{% endblock %}
